(let (/add (lambda (name) 
		(html
		 (tag "body" (list)
		      (tag "button" 
			   (list 
			    (list "onclick" 
				  (script 
				   (alert 
				    (string-append "hello " (from-server name)))))) 
			   "Click")))))
  (listen 9090))
