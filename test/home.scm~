(<html>
 (list (list "lang" "en"))
 (list
  (load "test/head.scm")
  (<body>
   (list)
   (list
    (<div>
     (list) 
     (list
      (load "test/header.scm")
      (load "test/form.scm")))
    (load "test/footer.scm")))))
