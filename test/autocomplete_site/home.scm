(<html>
 (list)
 (list
  (<body>
   (list)
   (list
    (<input>
     (list
      (list "id" "query")
      (list "onkeypress"
	    (js->string
	     (scheme->js
	      (print "coucou")))))
     (list))
    (<ul>
     (list (list "id" "response"))
     (list))))))
